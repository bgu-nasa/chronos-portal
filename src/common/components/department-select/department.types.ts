/**
 * Department types for common components
 * Minimal department information needed for selection components
 */

export interface Department {
    id: string;
    name: string;
    deleted: boolean;
}
