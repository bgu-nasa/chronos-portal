import type { ModuleConfig } from "@/infra";
import React from "react";

export const moduleConfig: ModuleConfig = {
    name: "Schedule",
    owner: "",
    basePath: "/schedule",
    routes: [],
    navigationItems: [],
};
